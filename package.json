{
  "name": "nfe-system",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "build": "npm run build -w @nfe/core && npm run build -w @nfe/xsd && npm run build -w @nfe/signer && npm run build -w @nfe/sefaz && npm run build -w @nfe/emission && npm run build -w @nfe/persistence",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build && node scripts/smoke.mjs",
    "smoke": "npm run build && node scripts/smoke.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.20.2",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.19.0",
    "vitest": "^2.1.8"
  }
}
